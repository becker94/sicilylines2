{% extends 'base.html.twig' %}

{% block title %}Traversee{% endblock %}

{% block body %}
    <h1>Traversee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ traversee.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ traversee.date ? traversee.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Heure</th>
                <td>{{ traversee.heure ? traversee.heure|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_traversee_index') }}">back to list</a>

    <a href="{{ path('app_traversee_edit', {'id': traversee.id}) }}">edit</a>

    {{ include('traversee/_delete_form.html.twig') }}
{% endblock %}
