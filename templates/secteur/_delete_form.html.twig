<form method="post" action="{{ path('app_secteur_delete', {'id': secteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ secteur.id) }}">
    <button class="btn">Delete</button>
</form>
