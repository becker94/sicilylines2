<form method="post" action="{{ path('app_liaison_periode_type_delete', {'id': liaison_periode_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liaison_periode_type.id) }}">
    <button class="btn">Delete</button>
</form>
