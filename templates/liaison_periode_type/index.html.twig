{% extends 'base.html.twig' %}

{% block title %}LiaisonPeriodeType index{% endblock %}

{% block body %}
    <h1>LiaisonPeriodeType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Tarif</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for liaison_periode_type in liaison_periode_types %}
            <tr>
                <td>{{ liaison_periode_type.id }}</td>
                <td>{{ liaison_periode_type.tarif }}</td>
                <td>
                    <a href="{{ path('app_liaison_periode_type_show', {'id': liaison_periode_type.id}) }}">show</a>
                    <a href="{{ path('app_liaison_periode_type_edit', {'id': liaison_periode_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_liaison_periode_type_new') }}">Create new</a>
{% endblock %}
